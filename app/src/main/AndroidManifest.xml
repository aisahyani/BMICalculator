<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignmentindividual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AssignmentIndividual">

        <activity android:name=".Profile"
            android:label="PROFILE"/>
        <activity android:name=".MainActivity"
            android:label="BMI CALCULATOR"/>
        <activity android:name=".Starting"
            android:theme="@style/Theme.AssignmentIndividual.NoActionBar"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>